const { Client, Message, Buttons } = require('../node_modules/whatsapp-web.js')
const nodemailer = require("nodemailer")

async function solicitaEPI(chatId, client){
    
}

async function reportSmartPhone(ChatId,client){

}

module.exports = {
    solicitaEPI,
    reportSmartPhone
}